# 08. 進捗管理

最終更新: 2026-07-23(pm)

## 1. フェーズステータス(設計書 docs/01 §8 対応)

| フェーズ | 内容(§8) | 状態 | 備考 |
|----------|-----------|------|------|
| Phase 0 | 暦マスタ生成+突合検証 / 診断エンジン実装+ユニットテスト | **完了** | エンジン全モジュール実装、テスト 255 件 green。暦マスタは yakumoin.info / NAOJ 突合検証に合格。qa-lead レビュー済み |
| Phase 1 | LIFF アプリ+API+NAS 環境構築 / キャラアセット import パイプライン | **実装完了・接続作業残**(受入未判定) | 実装・デプロイ基盤は完成。接続系の残タスク 4 件+実機テスト未実施(§3 参照)。import スクリプトはキャラリポジトリ待ち |
| Phase 2 | 夜間バッチ+LLM 文章生成+ジャーナル UI | 未着手 | Phase 1 受入後に着手 |
| Phase 3 | キャラデザイン適用・応答メッセージ体験・招待運用開始 | 未着手 | キャラアセット import はここまでに完了させればよい |

## 2. Phase 1 完了済み項目(2026-07-23 時点)

- API(Hono + SQLite): 認証・登録・診断・当日データ。Docker 化して NAS(UGREEN DXP4800GT)で稼働中
- 公開経路: Cloudflare Tunnel + iterra.online ドメイン(Xserver から NS 委譲)。https://mj-api.iterra.online 開通
- LIFF 全画面(登録・マイタイプ・今日・友達診断)+方位マップコンポーネント(Google Maps 抽象化レイヤ経由)
- フロントデプロイ: Cloudflare Workers 静的アセット(Git 連携ビルド)
- ローカル結合テスト合格(登録 → 診断 → 表示、検算値一致)
- 不具合修正: Docker ビルド重大バグ 3 件(tsbuildinfo 混入 / better-sqlite3 allowBuilds / workspace symlink)、MyTypePage の engine 出力形状不一致

## 3. 残タスク(Phase 1 クローズまで)

| # | タスク | 担当 | 実施場所 | ブロッカー / 前提 | 状態 |
|---|--------|------|----------|-------------------|------|
| 1 | NAS の api 再ビルド(CORS 修正 146ca02 の反映) | backend-dev(人間の LAN 内作業) | NAS(LAN 内) | 帰宅後に LAN 内から実施 | 未着手 |
| 2 | Cloudflare Workers 環境変数 `VITE_API_BASE_URL=https://mj-api.iterra.online` 設定+再デプロイ | liff-dev(人間のダッシュボード操作) | Cloudflare ダッシュボード | なし(Web から実施可) | 未着手 |
| 3 | LINE Developers で LIFF エンドポイント URL 設定 | 人間 | LINE Developers(Web) | #2 完了後が望ましい | 未着手 |
| 4 | 実機テスト(docs/09 チェックリスト) | 人間(チェックリストは qa-lead) | 実機(LINE アプリ) | #1〜#3 完了が前提。docs/09 は qa-lead が本日作成中 | 未着手 |
| 5 | キャラアセット import スクリプト(characters 1-12) | logic-dev または liff-dev | ローカル | キャラリポジトリ待ち。**Phase 3 まで保留可** | 保留 |

依存順序: #1・#2 は並行可 → #3 → #4。#5 は Phase 1 受入の必須条件から除外(Phase 3 の前提として管理)。

## 4. Phase 1 受入判定の状態

- 完了条件(§8): 「クローズド招待者が診断結果と今日の方位を見られる」— **未充足**(本番経路が未接続、実機未確認)
- 判定: 残タスク #1〜#4 完了+docs/09 の実機テスト合格をもって受入判定を行う
- 「完了」の定義はテスト合格・検証合格であり、実装完了ではない(楽観報告禁止)

## 5. 意思決定ログ

| 日付 | 決定 | 理由 |
|------|------|------|
| 2026-07-23 | iterra.jp は Cloudflare へ移管せず、専用ドメイン iterra.online を使用 | iterra.jp は Xserver DNS 運用のため移管リスクを回避 |
| 2026-07-22 | フロントは Cloudflare Pages ではなく Workers 静的アセットでデプロイ | Pages 構成との競合解消(_redirects 削除、wrangler.toml 再構成) |
| 2026-07-21 | 気学計算・比和・北基準・凶方位体系・数秘方式などの仕様確定 | 詳細は CLAUDE.md「確定済み仕様」参照(本書には再掲しない) |

## 6. ブロッカー・人間確認事項

| 事項 | 種別 | 状況 |
|------|------|------|
| NAS 再ビルド(LAN 内作業) | 物理的制約 | 帰宅後に実施予定 |
| キャラリポジトリの提供 | 外部待ち | Phase 3 までに必要 |
| 姓名判断の画数字体基準・吉凶表出典 | 仕様未確定(保留解除時) | Phase 0/1 のブロッカーではない(docs/10 §6 #4, #6) |
